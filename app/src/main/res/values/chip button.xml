<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="GoogleStyleChip" parent="Widget.MaterialComponents.Chip.Action">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="borderWidth">2dp</item>
       <!-- Text color (white in this example) -->
        <item name="android:background">@drawable/chipbutton</item> <!-- Background color (Google blue in this example) -->
        <item name="android:textSize">14sp</item> <!-- Text size (adjust as needed) -->
        <!-- Add other style attributes as needed -->
    </style>

</resources>

